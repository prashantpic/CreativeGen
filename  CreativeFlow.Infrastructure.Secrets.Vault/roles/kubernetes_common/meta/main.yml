---
galaxy_info:
  author: CreativeFlow AI
  description: Role for common Kubernetes node setup (containerd, kubelet, etc.).
  license: MIT
  min_ansible_version: "2.12"
  platforms:
    - name: Ubuntu
      versions:
        - "jammy" # 22.04
  galaxy_tags:
    - kubernetes
    - container

dependencies:
  - role: common