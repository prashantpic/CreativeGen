# This file contains production-specific secrets and MUST be encrypted using ansible-vault.
# Example: ansible-vault encrypt group_vars/production/secrets.yml
#
# These secrets will override any defined in group_vars/all/secrets.yml for the production environment.

# Production database password
# db_password: !vault |
#          $ANSIBLE_VAULT;1.1;AES256
#          ...

# Production SSL certificate private key
# ssl_private_key: !vault |
#          -----BEGIN PRIVATE KEY-----
#          ... (encrypted content)
#          -----END PRIVATE KEY-----